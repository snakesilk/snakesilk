{
  "name": "@snakesilk/platform-kit",
  "version": "0.4.1-1",
  "description": "Kit for building Platform games using Snakesilk.",
  "license": "MIT",
  "main": "./dist/index.js",
  "scripts": {
    "build": "babel ./src --out-dir ./dist",
    "prepublish": "npm run build",
    "test": "mocha './src/**/*-test.js'",
    "watch": "yarn run build -- --watch"
  },
  "dependencies": {
    "@snakesilk/engine": ">=0.14.0",
    "@snakesilk/platform-traits": ">=0.5.0",
    "@snakesilk/xml-loader": ">=0.12.1"
  }
}
